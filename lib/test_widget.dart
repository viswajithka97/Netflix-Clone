import 'package:flutter/material.dart';

class TestWidg extends StatelessWidget {
  const TestWidg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("HAi"),
    );
  }
}
